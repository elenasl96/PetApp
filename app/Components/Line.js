import * as React from "react";
import {
  StyleSheet,
  Text,
  View,
  Button,
  Image,
  TouchableHighlight,
  ScrollView,
} from "react-native";
export default function Line() {
  return (
    <View
      style={{
        borderBottomColor: "white",
        borderBottomWidth: 1,
      }}
    />
  );
}
